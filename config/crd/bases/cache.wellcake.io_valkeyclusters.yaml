---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: valkeyclusters.cache.wellcake.io
spec:
  group: cache.wellcake.io
  names:
    kind: ValkeyCluster
    listKind: ValkeyClusterList
    plural: valkeyclusters
    shortNames:
    - vk
    - vkc
    singular: valkeycluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.topology
      name: Topology
      type: string
    - jsonPath: .spec.replicas
      name: Replicas
      type: integer
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ValkeyCluster is the Schema for the valkeyclusters API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ValkeyClusterSpec defines the desired state of ValkeyCluster.
            properties:
              affinity:
                description: Affinity for Valkey pods. If nil the operator applies
                  pod anti-affinity by default.
                properties:
                  nodeAffinity:
                    description: Describes node affinity scheduling rules for the
                      pod.
                    properties:
                      preferredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          The scheduler will prefer to schedule pods to nodes that satisfy
                          the affinity expressions specified by this field, but it may choose
                          a node that violates one or more of the expressions. The node that is
                          most preferred is the one with the greatest sum of weights, i.e.
                          for each node that meets all of the scheduling requirements (resource
                          request, requiredDuringScheduling affinity expressions, etc.),
                          compute a sum by iterating through the elements of this field and adding
                          "weight" to the sum if the node matches the corresponding matchExpressions; the
                          node(s) with the highest sum are the most preferred.
                        items:
                          description: |-
                            An empty preferred scheduling term matches all objects with implicit weight 0
                            (i.e. it's a no-op). A null preferred scheduling term matches no objects (i.e. is also a no-op).
                          properties:
                            preference:
                              description: A node selector term, associated with the
                                corresponding weight.
                              properties:
                                matchExpressions:
                                  description: A list of node selector requirements
                                    by node's labels.
                                  items:
                                    description: |-
                                      A node selector requirement is a selector that contains values, a key, and an operator
                                      that relates the key and values.
                                    properties:
                                      key:
                                        description: The label key that the selector
                                          applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          Represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                        type: string
                                      values:
                                        description: |-
                                          An array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. If the operator is Gt or Lt, the values
                                          array must have a single element, which will be interpreted as an integer.
                                          This array is replaced during a strategic merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchFields:
                                  description: A list of node selector requirements
                                    by node's fields.
                                  items:
                                    description: |-
                                      A node selector requirement is a selector that contains values, a key, and an operator
                                      that relates the key and values.
                                    properties:
                                      key:
                                        description: The label key that the selector
                                          applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          Represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                        type: string
                                      values:
                                        description: |-
                                          An array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. If the operator is Gt or Lt, the values
                                          array must have a single element, which will be interpreted as an integer.
                                          This array is replaced during a strategic merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                              type: object
                              x-kubernetes-map-type: atomic
                            weight:
                              description: Weight associated with matching the corresponding
                                nodeSelectorTerm, in the range 1-100.
                              format: int32
                              type: integer
                          required:
                          - preference
                          - weight
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      requiredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          If the affinity requirements specified by this field are not met at
                          scheduling time, the pod will not be scheduled onto the node.
                          If the affinity requirements specified by this field cease to be met
                          at some point during pod execution (e.g. due to an update), the system
                          may or may not try to eventually evict the pod from its node.
                        properties:
                          nodeSelectorTerms:
                            description: Required. A list of node selector terms.
                              The terms are ORed.
                            items:
                              description: |-
                                A null or empty node selector term matches no objects. The requirements of
                                them are ANDed.
                                The TopologySelectorTerm type implements a subset of the NodeSelectorTerm.
                              properties:
                                matchExpressions:
                                  description: A list of node selector requirements
                                    by node's labels.
                                  items:
                                    description: |-
                                      A node selector requirement is a selector that contains values, a key, and an operator
                                      that relates the key and values.
                                    properties:
                                      key:
                                        description: The label key that the selector
                                          applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          Represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                        type: string
                                      values:
                                        description: |-
                                          An array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. If the operator is Gt or Lt, the values
                                          array must have a single element, which will be interpreted as an integer.
                                          This array is replaced during a strategic merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchFields:
                                  description: A list of node selector requirements
                                    by node's fields.
                                  items:
                                    description: |-
                                      A node selector requirement is a selector that contains values, a key, and an operator
                                      that relates the key and values.
                                    properties:
                                      key:
                                        description: The label key that the selector
                                          applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          Represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                        type: string
                                      values:
                                        description: |-
                                          An array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. If the operator is Gt or Lt, the values
                                          array must have a single element, which will be interpreted as an integer.
                                          This array is replaced during a strategic merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                              type: object
                              x-kubernetes-map-type: atomic
                            type: array
                            x-kubernetes-list-type: atomic
                        required:
                        - nodeSelectorTerms
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  podAffinity:
                    description: Describes pod affinity scheduling rules (e.g. co-locate
                      this pod in the same node, zone, etc. as some other pod(s)).
                    properties:
                      preferredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          The scheduler will prefer to schedule pods to nodes that satisfy
                          the affinity expressions specified by this field, but it may choose
                          a node that violates one or more of the expressions. The node that is
                          most preferred is the one with the greatest sum of weights, i.e.
                          for each node that meets all of the scheduling requirements (resource
                          request, requiredDuringScheduling affinity expressions, etc.),
                          compute a sum by iterating through the elements of this field and adding
                          "weight" to the sum if the node has pods which matches the corresponding podAffinityTerm; the
                          node(s) with the highest sum are the most preferred.
                        items:
                          description: The weights of all of the matched WeightedPodAffinityTerm
                            fields are added per-node to find the most preferred node(s)
                          properties:
                            podAffinityTerm:
                              description: Required. A pod affinity term, associated
                                with the corresponding weight.
                              properties:
                                labelSelector:
                                  description: |-
                                    A label query over a set of resources, in this case pods.
                                    If it's null, this PodAffinityTerm matches with no Pods.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                matchLabelKeys:
                                  description: |-
                                    MatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                    Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                mismatchLabelKeys:
                                  description: |-
                                    MismatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                    Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                namespaceSelector:
                                  description: |-
                                    A label query over the set of namespaces that the term applies to.
                                    The term is applied to the union of the namespaces selected by this field
                                    and the ones listed in the namespaces field.
                                    null selector and null or empty namespaces list means "this pod's namespace".
                                    An empty selector ({}) matches all namespaces.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: |-
                                    namespaces specifies a static list of namespace names that the term applies to.
                                    The term is applied to the union of the namespaces listed in this field
                                    and the ones selected by namespaceSelector.
                                    null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                topologyKey:
                                  description: |-
                                    This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                    the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                    whose value of the label with key topologyKey matches that of any node on which any of the
                                    selected pods is running.
                                    Empty topologyKey is not allowed.
                                  type: string
                              required:
                              - topologyKey
                              type: object
                            weight:
                              description: |-
                                weight associated with matching the corresponding podAffinityTerm,
                                in the range 1-100.
                              format: int32
                              type: integer
                          required:
                          - podAffinityTerm
                          - weight
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      requiredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          If the affinity requirements specified by this field are not met at
                          scheduling time, the pod will not be scheduled onto the node.
                          If the affinity requirements specified by this field cease to be met
                          at some point during pod execution (e.g. due to a pod label update), the
                          system may or may not try to eventually evict the pod from its node.
                          When there are multiple elements, the lists of nodes corresponding to each
                          podAffinityTerm are intersected, i.e. all terms must be satisfied.
                        items:
                          description: |-
                            Defines a set of pods (namely those matching the labelSelector
                            relative to the given namespace(s)) that this pod should be
                            co-located (affinity) or not co-located (anti-affinity) with,
                            where co-located is defined as running on a node whose value of
                            the label with key <topologyKey> matches that of any node on which
                            a pod of the set of pods is running
                          properties:
                            labelSelector:
                              description: |-
                                A label query over a set of resources, in this case pods.
                                If it's null, this PodAffinityTerm matches with no Pods.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            matchLabelKeys:
                              description: |-
                                MatchLabelKeys is a set of pod label keys to select which pods will
                                be taken into consideration. The keys are used to lookup values from the
                                incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                to select the group of existing pods which pods will be taken into consideration
                                for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                pod labels will be ignored. The default value is empty.
                                The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                Also, matchLabelKeys cannot be set when labelSelector isn't set.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            mismatchLabelKeys:
                              description: |-
                                MismatchLabelKeys is a set of pod label keys to select which pods will
                                be taken into consideration. The keys are used to lookup values from the
                                incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                to select the group of existing pods which pods will be taken into consideration
                                for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                pod labels will be ignored. The default value is empty.
                                The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            namespaceSelector:
                              description: |-
                                A label query over the set of namespaces that the term applies to.
                                The term is applied to the union of the namespaces selected by this field
                                and the ones listed in the namespaces field.
                                null selector and null or empty namespaces list means "this pod's namespace".
                                An empty selector ({}) matches all namespaces.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            namespaces:
                              description: |-
                                namespaces specifies a static list of namespace names that the term applies to.
                                The term is applied to the union of the namespaces listed in this field
                                and the ones selected by namespaceSelector.
                                null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            topologyKey:
                              description: |-
                                This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                whose value of the label with key topologyKey matches that of any node on which any of the
                                selected pods is running.
                                Empty topologyKey is not allowed.
                              type: string
                          required:
                          - topologyKey
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                    type: object
                  podAntiAffinity:
                    description: Describes pod anti-affinity scheduling rules (e.g.
                      avoid putting this pod in the same node, zone, etc. as some
                      other pod(s)).
                    properties:
                      preferredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          The scheduler will prefer to schedule pods to nodes that satisfy
                          the anti-affinity expressions specified by this field, but it may choose
                          a node that violates one or more of the expressions. The node that is
                          most preferred is the one with the greatest sum of weights, i.e.
                          for each node that meets all of the scheduling requirements (resource
                          request, requiredDuringScheduling anti-affinity expressions, etc.),
                          compute a sum by iterating through the elements of this field and subtracting
                          "weight" from the sum if the node has pods which matches the corresponding podAffinityTerm; the
                          node(s) with the highest sum are the most preferred.
                        items:
                          description: The weights of all of the matched WeightedPodAffinityTerm
                            fields are added per-node to find the most preferred node(s)
                          properties:
                            podAffinityTerm:
                              description: Required. A pod affinity term, associated
                                with the corresponding weight.
                              properties:
                                labelSelector:
                                  description: |-
                                    A label query over a set of resources, in this case pods.
                                    If it's null, this PodAffinityTerm matches with no Pods.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                matchLabelKeys:
                                  description: |-
                                    MatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                    Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                mismatchLabelKeys:
                                  description: |-
                                    MismatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                    Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                namespaceSelector:
                                  description: |-
                                    A label query over the set of namespaces that the term applies to.
                                    The term is applied to the union of the namespaces selected by this field
                                    and the ones listed in the namespaces field.
                                    null selector and null or empty namespaces list means "this pod's namespace".
                                    An empty selector ({}) matches all namespaces.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: |-
                                    namespaces specifies a static list of namespace names that the term applies to.
                                    The term is applied to the union of the namespaces listed in this field
                                    and the ones selected by namespaceSelector.
                                    null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                topologyKey:
                                  description: |-
                                    This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                    the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                    whose value of the label with key topologyKey matches that of any node on which any of the
                                    selected pods is running.
                                    Empty topologyKey is not allowed.
                                  type: string
                              required:
                              - topologyKey
                              type: object
                            weight:
                              description: |-
                                weight associated with matching the corresponding podAffinityTerm,
                                in the range 1-100.
                              format: int32
                              type: integer
                          required:
                          - podAffinityTerm
                          - weight
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      requiredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          If the anti-affinity requirements specified by this field are not met at
                          scheduling time, the pod will not be scheduled onto the node.
                          If the anti-affinity requirements specified by this field cease to be met
                          at some point during pod execution (e.g. due to a pod label update), the
                          system may or may not try to eventually evict the pod from its node.
                          When there are multiple elements, the lists of nodes corresponding to each
                          podAffinityTerm are intersected, i.e. all terms must be satisfied.
                        items:
                          description: |-
                            Defines a set of pods (namely those matching the labelSelector
                            relative to the given namespace(s)) that this pod should be
                            co-located (affinity) or not co-located (anti-affinity) with,
                            where co-located is defined as running on a node whose value of
                            the label with key <topologyKey> matches that of any node on which
                            a pod of the set of pods is running
                          properties:
                            labelSelector:
                              description: |-
                                A label query over a set of resources, in this case pods.
                                If it's null, this PodAffinityTerm matches with no Pods.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            matchLabelKeys:
                              description: |-
                                MatchLabelKeys is a set of pod label keys to select which pods will
                                be taken into consideration. The keys are used to lookup values from the
                                incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                to select the group of existing pods which pods will be taken into consideration
                                for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                pod labels will be ignored. The default value is empty.
                                The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                Also, matchLabelKeys cannot be set when labelSelector isn't set.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            mismatchLabelKeys:
                              description: |-
                                MismatchLabelKeys is a set of pod label keys to select which pods will
                                be taken into consideration. The keys are used to lookup values from the
                                incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                to select the group of existing pods which pods will be taken into consideration
                                for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                pod labels will be ignored. The default value is empty.
                                The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            namespaceSelector:
                              description: |-
                                A label query over the set of namespaces that the term applies to.
                                The term is applied to the union of the namespaces selected by this field
                                and the ones listed in the namespaces field.
                                null selector and null or empty namespaces list means "this pod's namespace".
                                An empty selector ({}) matches all namespaces.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            namespaces:
                              description: |-
                                namespaces specifies a static list of namespace names that the term applies to.
                                The term is applied to the union of the namespaces listed in this field
                                and the ones selected by namespaceSelector.
                                null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            topologyKey:
                              description: |-
                                This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                whose value of the label with key topologyKey matches that of any node on which any of the
                                selected pods is running.
                                Empty topologyKey is not allowed.
                              type: string
                          required:
                          - topologyKey
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                    type: object
                type: object
              auth:
                description: Auth configures password authentication.
                properties:
                  enabled:
                    default: true
                    description: Enabled toggles requirepass.
                    type: boolean
                  existingSecret:
                    description: |-
                      ExistingSecret holds the password. If empty, the operator generates one.
                      The secret must contain key "password".
                    type: string
                type: object
              autoReshard:
                default: true
                description: |-
                  AutoReshard, when true and topology=Cluster, makes scale-up runs also
                  invoke `valkey-cli --cluster rebalance --cluster-use-empty-masters` so
                  new shards receive their share of slots automatically, and makes
                  scale-down rebalance the surviving masters after slots are migrated off
                  the leaving nodes. Defaults to true so a Crossplane/GitOps-driven change
                  to `shards` converges to a balanced, fully-covered cluster with no manual
                  `valkey-cli --cluster reshard`. Set false to keep slot (re)distribution a
                  deliberate manual step.
                type: boolean
              backup:
                description: Backup configures periodic RDB snapshot upload to S3-compatible
                  storage.
                properties:
                  awsCLIImage:
                    default: amazon/aws-cli:2.15.0
                    description: AWSCLIImage override for the upload step.
                    type: string
                  enabled:
                    description: Enabled toggles the CronJob.
                    type: boolean
                  image:
                    description: |-
                      Image override for the dump step. Defaults to spec.image (Valkey image
                      includes valkey-cli).
                    type: string
                  retention:
                    default: 7
                    description: |-
                      Retention is the number of most-recent snapshots to keep under the S3
                      prefix. Older snapshots matching `<prefix><cluster>-*.rdb` are deleted
                      after each successful upload. 0 disables retention (keep forever).
                    format: int32
                    minimum: 0
                    type: integer
                  s3:
                    description: S3 holds destination details.
                    properties:
                      bucket:
                        description: Bucket name.
                        minLength: 1
                        type: string
                      credentialsSecret:
                        description: |-
                          CredentialsSecret references a Secret with keys
                          AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY.
                        minLength: 1
                        type: string
                      encryption:
                        description: |-
                          Encryption selects server-side encryption for uploads.
                            AES256 — SSE-S3, AWS-managed keys (no extra setup, no extra cost)
                            KMS    — SSE-KMS, customer-controlled key in KMSKeyID (translated to
                                     "aws:kms" when invoking the AWS CLI).
                          Empty disables SSE on upload. Reading SSE-S3 objects is transparent;
                          reading SSE-KMS objects requires the same KMS permissions on the
                          reading principal (so the restore Secret must have kms:Decrypt on the
                          key).
                        enum:
                        - AES256
                        - KMS
                        type: string
                      endpoint:
                        description: Endpoint URL for S3-compatible services. Leave
                          empty for AWS S3.
                        type: string
                      kmsKeyId:
                        description: KMSKeyID is the KMS key ARN/ID/alias when Encryption=aws:kms.
                        type: string
                      prefix:
                        description: |-
                          Prefix is prepended to the object key. The final key is
                            <prefix>/<cluster-name>-YYYYMMDD-HHMMSS.rdb
                        type: string
                      region:
                        default: us-east-1
                        description: Region, defaults to "us-east-1" for AWS / empty
                          for many MinIO setups.
                        type: string
                    required:
                    - bucket
                    - credentialsSecret
                    type: object
                  schedule:
                    default: 0 3 * * *
                    description: Schedule is a standard cron expression, e.g. "0 3
                      * * *".
                    type: string
                required:
                - s3
                type: object
              config:
                additionalProperties:
                  type: string
                description: Config is a free-form map of Valkey config directives
                  appended to valkey.conf.
                type: object
              image:
                default: valkey/valkey:8.0
                description: Image is the Valkey container image. Defaults to a known
                  stable tag.
                type: string
              imagePullPolicy:
                default: IfNotPresent
                description: ImagePullPolicy for the Valkey container.
                type: string
              metrics:
                description: Metrics enables the Prometheus exporter sidecar.
                properties:
                  enabled:
                    description: Enabled toggles the exporter sidecar.
                    type: boolean
                  image:
                    default: oliver006/redis_exporter:v1.62.0
                    description: Image of the exporter.
                    type: string
                  serviceMonitor:
                    description: ServiceMonitor creates a Prometheus Operator ServiceMonitor.
                    type: boolean
                type: object
              networkPolicy:
                description: NetworkPolicy generates a default-deny + allow-from policy
                  if Enabled.
                properties:
                  allowFrom:
                    description: |-
                      AllowFrom is a list of pod/namespace selectors allowed to reach Valkey ports.
                      If empty, only pods in the same namespace are allowed.
                    items:
                      description: |-
                        NetworkPolicyPeer describes a peer to allow traffic to/from. Only certain combinations of
                        fields are allowed
                      properties:
                        ipBlock:
                          description: |-
                            ipBlock defines policy on a particular IPBlock. If this field is set then
                            neither of the other fields can be.
                          properties:
                            cidr:
                              description: |-
                                cidr is a string representing the IPBlock
                                Valid examples are "192.168.1.0/24" or "2001:db8::/64"
                              type: string
                            except:
                              description: |-
                                except is a slice of CIDRs that should not be included within an IPBlock
                                Valid examples are "192.168.1.0/24" or "2001:db8::/64"
                                Except values will be rejected if they are outside the cidr range
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - cidr
                          type: object
                        namespaceSelector:
                          description: |-
                            namespaceSelector selects namespaces using cluster-scoped labels. This field follows
                            standard label selector semantics; if present but empty, it selects all namespaces.

                            If podSelector is also set, then the NetworkPolicyPeer as a whole selects
                            the pods matching podSelector in the namespaces selected by namespaceSelector.
                            Otherwise it selects all pods in the namespaces selected by namespaceSelector.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        podSelector:
                          description: |-
                            podSelector is a label selector which selects pods. This field follows standard label
                            selector semantics; if present but empty, it selects all pods.

                            If namespaceSelector is also set, then the NetworkPolicyPeer as a whole selects
                            the pods matching podSelector in the Namespaces selected by NamespaceSelector.
                            Otherwise it selects the pods matching podSelector in the policy's own namespace.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                    type: array
                  enabled:
                    description: Enabled toggles NetworkPolicy generation. Defaults
                      to false (opt-in for now).
                    type: boolean
                type: object
              nodeSelector:
                additionalProperties:
                  type: string
                description: NodeSelector for Valkey pods.
                type: object
              perShardWorkload:
                description: |-
                  PerShardWorkload, when true (Cluster topology only), renders ONE
                  StatefulSet per shard instead of a single StatefulSet for the whole
                  cluster (ADR 0005). Unlocks shard-aware anti-affinity (a shard's
                  primary+replicas on different nodes — not expressible from one shared pod
                  template) and clean shard scale-down (remove a shard = delete its STS).
                  Immutable (see the spec-level rule). EXPERIMENTAL; default (absent →
                  false) keeps the single-StatefulSet path. Mirrors v1beta1 so the field
                  survives v1alpha1↔v1beta1 conversion (the public Crossplane composition
                  writes v1alpha1).
                type: boolean
              podDisruptionBudget:
                description: |-
                  PodDisruptionBudget controls voluntary disruptions for the Valkey StatefulSet.
                  Defaults to MaxUnavailable=1 when nil.
                properties:
                  enabled:
                    default: true
                    description: Enabled toggles the PDB. Defaults to true.
                    type: boolean
                  maxUnavailable:
                    anyOf:
                    - type: integer
                    - type: string
                    description: MaxUnavailable, mutually exclusive with MinAvailable.
                      Default "1".
                    x-kubernetes-int-or-string: true
                  minAvailable:
                    anyOf:
                    - type: integer
                    - type: string
                    description: MinAvailable, mutually exclusive with MaxUnavailable.
                    x-kubernetes-int-or-string: true
                type: object
              profile:
                default: Cache
                description: |-
                  Profile selects the workload profile (Cache or Durable). Immutable —
                  changing profile would require re-sizing PVCs and flipping persistence,
                  which is not safe in-place. Drives defaults for persistence, eviction
                  policy and maxmemory headroom.
                enum:
                - Cache
                - Durable
                type: string
                x-kubernetes-validations:
                - message: profile is immutable
                  rule: self == oldSelf
              replicas:
                default: 3
                description: |-
                  Replicas is the number of Valkey instances.
                  For Replication: 1 primary + (Replicas-1) replicas. For Cluster: total nodes (must be >=6 with Shards*ReplicasPerShard).
                format: int32
                minimum: 1
                type: integer
              replicasPerShard:
                description: ReplicasPerShard for Cluster topology — replicas per
                  primary.
                format: int32
                minimum: 0
                type: integer
              replicateFrom:
                description: |-
                  ReplicateFrom makes this cluster a pull-based asynchronous replica of
                  an external Valkey/Redis primary, usually in another region or another
                  Kubernetes cluster — a DR / read-scaling pattern.

                  When set, every pod (including pod-0) runs `replicaof <host> <port>`
                  at startup, and operator-driven local failover is disabled (the local
                  pods are all replicas of an external source). Promotion on DR is a
                  manual step: clear spec.replicateFrom, the operator then lets pod-0
                  resume primary role and replicas attach to it.

                  Supported topologies: Standalone, Replication.
                  Sentinel and Cluster are unsupported: Sentinel would race the local
                  quorum against the external primary, and OSS Valkey Cluster has no
                  cross-cluster replication semantics.
                properties:
                  caSecret:
                    description: |-
                      CASecret references a Secret holding the source cluster's CA bundle (PEM)
                      to verify the upstream primary's TLS cert when it is signed by a separate
                      CA — the multi-region case (S4). The operator merges it with this
                      cluster's own CA into one trust bundle. Key defaults to "ca.crt".
                    properties:
                      key:
                        default: ca.crt
                        description: Key in the Secret whose value is the PEM CA bundle.
                          Defaults to "ca.crt".
                        type: string
                      name:
                        description: Name of the Secret holding the source cluster's
                          CA bundle.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  host:
                    description: |-
                      Host is the external primary FQDN or IP — typically a LoadBalancer
                      service, a Sentinel-discovered address, or a peered ClusterIP.
                    minLength: 1
                    type: string
                  passwordSecret:
                    description: |-
                      PasswordSecret references the source cluster's auth password,
                      used as masterauth on this cluster. If unset, replication is
                      attempted without auth.
                    properties:
                      key:
                        default: password
                        type: string
                      name:
                        type: string
                    required:
                    - name
                    type: object
                  port:
                    default: 6379
                    description: Port of the external primary. Defaults to 6379.
                    format: int32
                    type: integer
                  tls:
                    description: |-
                      TLS indicates the upstream primary accepts TLS connections. When true,
                      replication frames are encrypted (`tls-replication yes`). The upstream
                      certificate must chain to a CA this cluster trusts. In a multi-region
                      setup where the source has its own CA (different from this cluster's),
                      set caSecret below — the operator then trusts both. If source and target
                      share a CA, leave caSecret empty.
                    type: boolean
                required:
                - host
                type: object
              resources:
                description: Resources for the Valkey container.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              restoreFrom:
                description: |-
                  RestoreFrom downloads an RDB from S3 into /data/dump.rdb on each pod
                  before Valkey starts, but ONLY when /data/dump.rdb does not already
                  exist. This makes restore a no-op once a cluster has been bootstrapped:
                  to re-restore, delete /data/dump.rdb via a debug pod first.

                  Standalone/Replication/Sentinel: sourceKey is used verbatim on every pod;
                  replicas resync from the primary after it loads the RDB.

                  Cluster: restore is per-shard — sourceKey must contain a "{shard}"
                  placeholder; each master ordinal pulls its own snapshot. The operator does
                  NOT auto-run `valkey-cli --cluster create` in this case (it aborts on
                  non-empty nodes); cluster assembly is a guided manual procedure
                  (docs/runbook.md#cluster-restore), after which the operator detects the
                  formed cluster and marks it initialized.
                properties:
                  awsCLIImage:
                    default: amazon/aws-cli:2.15.0
                    description: AWSCLIImage override for the download init container.
                    type: string
                  s3:
                    description: |-
                      S3 destination details. Reuses the same shape as backup.s3, including
                      the credentials Secret. The credentialsSecret may be the same Secret
                      used by Backup, or a separate read-only one.
                    properties:
                      bucket:
                        description: Bucket name.
                        minLength: 1
                        type: string
                      credentialsSecret:
                        description: |-
                          CredentialsSecret references a Secret with keys
                          AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY.
                        minLength: 1
                        type: string
                      encryption:
                        description: |-
                          Encryption selects server-side encryption for uploads.
                            AES256 — SSE-S3, AWS-managed keys (no extra setup, no extra cost)
                            KMS    — SSE-KMS, customer-controlled key in KMSKeyID (translated to
                                     "aws:kms" when invoking the AWS CLI).
                          Empty disables SSE on upload. Reading SSE-S3 objects is transparent;
                          reading SSE-KMS objects requires the same KMS permissions on the
                          reading principal (so the restore Secret must have kms:Decrypt on the
                          key).
                        enum:
                        - AES256
                        - KMS
                        type: string
                      endpoint:
                        description: Endpoint URL for S3-compatible services. Leave
                          empty for AWS S3.
                        type: string
                      kmsKeyId:
                        description: KMSKeyID is the KMS key ARN/ID/alias when Encryption=aws:kms.
                        type: string
                      prefix:
                        description: |-
                          Prefix is prepended to the object key. The final key is
                            <prefix>/<cluster-name>-YYYYMMDD-HHMMSS.rdb
                        type: string
                      region:
                        default: us-east-1
                        description: Region, defaults to "us-east-1" for AWS / empty
                          for many MinIO setups.
                        type: string
                    required:
                    - bucket
                    - credentialsSecret
                    type: object
                  sourceKey:
                    description: |-
                      SourceKey is the S3 key to download.

                      Single-shard topologies (Standalone/Replication/Sentinel) use it verbatim,
                      e.g. "backups/web-cache-20260528-030000.rdb".

                      For Cluster topology the restore is per-shard: SourceKey must contain a
                      "{shard}" placeholder which each master ordinal (0..shards-1) substitutes
                      with its index, e.g. "backups/web-20260528-030000-shard-{shard}.rdb".
                      Cluster restore requires the same shard count as the snapshot and is a
                      guided manual procedure — see docs/runbook.md#cluster-restore.
                    minLength: 1
                    type: string
                required:
                - s3
                - sourceKey
                type: object
              sentinel:
                description: Sentinel-specific settings (only used when Topology=Sentinel).
                properties:
                  image:
                    description: Image of the Sentinel container. Defaults to the
                      same image as Valkey.
                    type: string
                  quorum:
                    description: Quorum override. If zero, computed as Replicas/2
                      + 1.
                    format: int32
                    type: integer
                  replicas:
                    default: 3
                    description: Replicas is the number of Sentinel pods. Quorum is
                      Replicas/2 + 1.
                    format: int32
                    minimum: 3
                    type: integer
                type: object
              shards:
                description: Shards is only used for Cluster topology. Number of shards
                  (primaries).
                format: int32
                minimum: 3
                type: integer
              storage:
                description: Storage configures persistence backed by PVCs.
                properties:
                  mode:
                    default: rdb
                    description: 'Persistence mode: rdb, aof, both, none.'
                    enum:
                    - rdb
                    - aof
                    - both
                    - none
                    type: string
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 10Gi
                    description: |-
                      Size of each PVC, e.g. "10Gi". Expand-only: the value may grow but never
                      shrink (PVC expansion in k8s only works upwards anyway, and only if the
                      StorageClass has AllowVolumeExpansion=true).
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                    x-kubernetes-validations:
                    - message: storage size cannot shrink
                      rule: quantity(self).compareTo(quantity(oldSelf)) >= 0
                  storageClassName:
                    description: |-
                      StorageClassName for the PVC. Immutable: changing the StorageClass on an
                      existing StatefulSet has no effect on already-bound PVCs and can lead to
                      inconsistent storage between pod ordinals.
                    type: string
                    x-kubernetes-validations:
                    - message: storageClassName is immutable
                      rule: self == oldSelf
                type: object
              tls:
                description: TLS configures TLS via cert-manager.
                properties:
                  enabled:
                    description: Enabled toggles TLS for client and replication traffic.
                    type: boolean
                  existingSecret:
                    description: |-
                      ExistingSecret is a pre-created secret containing tls.crt, tls.key, ca.crt.
                      Either IssuerRef or ExistingSecret must be set when Enabled=true.
                    type: string
                  issuerRef:
                    description: IssuerRef points at a cert-manager Issuer or ClusterIssuer.
                    properties:
                      group:
                        type: string
                      kind:
                        default: Issuer
                        enum:
                        - Issuer
                        - ClusterIssuer
                        type: string
                      name:
                        type: string
                    required:
                    - name
                    type: object
                  mutualTLS:
                    description: |-
                      MutualTLS, when true, requires clients to present a certificate signed by
                      the cluster CA (`tls-auth-clients yes`). Default false keeps client certs
                      optional (`tls-auth-clients optional`) — pod-to-pod replication always
                      uses the mounted cert regardless. Set true to enforce mTLS, as the
                      platform DBaaS contract does.
                    type: boolean
                type: object
              tolerations:
                description: Tolerations for Valkey pods.
                items:
                  description: |-
                    The pod this Toleration is attached to tolerates any taint that matches
                    the triple <key,value,effect> using the matching operator <operator>.
                  properties:
                    effect:
                      description: |-
                        Effect indicates the taint effect to match. Empty means match all taint effects.
                        When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                      type: string
                    key:
                      description: |-
                        Key is the taint key that the toleration applies to. Empty means match all taint keys.
                        If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                      type: string
                    operator:
                      description: |-
                        Operator represents a key's relationship to the value.
                        Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                        Exists is equivalent to wildcard for value, so that a pod can
                        tolerate all taints of a particular category.
                        Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                      type: string
                    tolerationSeconds:
                      description: |-
                        TolerationSeconds represents the period of time the toleration (which must be
                        of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                        it is not set, which means tolerate the taint forever (do not evict). Zero and
                        negative values will be treated as 0 (evict immediately) by the system.
                      format: int64
                      type: integer
                    value:
                      description: |-
                        Value is the taint value the toleration matches to.
                        If the operator is Exists, the value should be empty, otherwise just a regular string.
                      type: string
                  type: object
                type: array
              topology:
                default: Replication
                description: |-
                  Topology selects the deployment shape. Immutable after creation —
                  switching shape requires a new resource because PVCs, layout and
                  bootstrap differ fundamentally.
                enum:
                - Standalone
                - Replication
                - Sentinel
                - Cluster
                type: string
                x-kubernetes-validations:
                - message: topology is immutable
                  rule: self == oldSelf
              topologySpreadConstraints:
                description: |-
                  TopologySpreadConstraints to spread pods across zones/DCs.
                  If nil the operator applies a default hostname spread across availability zones.
                items:
                  description: TopologySpreadConstraint specifies how to spread matching
                    pods among the given topology.
                  properties:
                    labelSelector:
                      description: |-
                        LabelSelector is used to find matching pods.
                        Pods that match this label selector are counted to determine the number of pods
                        in their corresponding topology domain.
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                    matchLabelKeys:
                      description: |-
                        MatchLabelKeys is a set of pod label keys to select the pods over which
                        spreading will be calculated. The keys are used to lookup values from the
                        incoming pod labels, those key-value labels are ANDed with labelSelector
                        to select the group of existing pods over which spreading will be calculated
                        for the incoming pod. The same key is forbidden to exist in both MatchLabelKeys and LabelSelector.
                        MatchLabelKeys cannot be set when LabelSelector isn't set.
                        Keys that don't exist in the incoming pod labels will
                        be ignored. A null or empty list means only match against labelSelector.

                        This is a beta field and requires the MatchLabelKeysInPodTopologySpread feature gate to be enabled (enabled by default).
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                    maxSkew:
                      description: |-
                        MaxSkew describes the degree to which pods may be unevenly distributed.
                        When `whenUnsatisfiable=DoNotSchedule`, it is the maximum permitted difference
                        between the number of matching pods in the target topology and the global minimum.
                        The global minimum is the minimum number of matching pods in an eligible domain
                        or zero if the number of eligible domains is less than MinDomains.
                        For example, in a 3-zone cluster, MaxSkew is set to 1, and pods with the same
                        labelSelector spread as 2/2/1:
                        In this case, the global minimum is 1.
                        | zone1 | zone2 | zone3 |
                        |  P P  |  P P  |   P   |
                        - if MaxSkew is 1, incoming pod can only be scheduled to zone3 to become 2/2/2;
                        scheduling it onto zone1(zone2) would make the ActualSkew(3-1) on zone1(zone2)
                        violate MaxSkew(1).
                        - if MaxSkew is 2, incoming pod can be scheduled onto any zone.
                        When `whenUnsatisfiable=ScheduleAnyway`, it is used to give higher precedence
                        to topologies that satisfy it.
                        It's a required field. Default value is 1 and 0 is not allowed.
                      format: int32
                      type: integer
                    minDomains:
                      description: |-
                        MinDomains indicates a minimum number of eligible domains.
                        When the number of eligible domains with matching topology keys is less than minDomains,
                        Pod Topology Spread treats "global minimum" as 0, and then the calculation of Skew is performed.
                        And when the number of eligible domains with matching topology keys equals or greater than minDomains,
                        this value has no effect on scheduling.
                        As a result, when the number of eligible domains is less than minDomains,
                        scheduler won't schedule more than maxSkew Pods to those domains.
                        If value is nil, the constraint behaves as if MinDomains is equal to 1.
                        Valid values are integers greater than 0.
                        When value is not nil, WhenUnsatisfiable must be DoNotSchedule.

                        For example, in a 3-zone cluster, MaxSkew is set to 2, MinDomains is set to 5 and pods with the same
                        labelSelector spread as 2/2/2:
                        | zone1 | zone2 | zone3 |
                        |  P P  |  P P  |  P P  |
                        The number of domains is less than 5(MinDomains), so "global minimum" is treated as 0.
                        In this situation, new pod with the same labelSelector cannot be scheduled,
                        because computed skew will be 3(3 - 0) if new Pod is scheduled to any of the three zones,
                        it will violate MaxSkew.
                      format: int32
                      type: integer
                    nodeAffinityPolicy:
                      description: |-
                        NodeAffinityPolicy indicates how we will treat Pod's nodeAffinity/nodeSelector
                        when calculating pod topology spread skew. Options are:
                        - Honor: only nodes matching nodeAffinity/nodeSelector are included in the calculations.
                        - Ignore: nodeAffinity/nodeSelector are ignored. All nodes are included in the calculations.

                        If this value is nil, the behavior is equivalent to the Honor policy.
                      type: string
                    nodeTaintsPolicy:
                      description: |-
                        NodeTaintsPolicy indicates how we will treat node taints when calculating
                        pod topology spread skew. Options are:
                        - Honor: nodes without taints, along with tainted nodes for which the incoming pod
                        has a toleration, are included.
                        - Ignore: node taints are ignored. All nodes are included.

                        If this value is nil, the behavior is equivalent to the Ignore policy.
                      type: string
                    topologyKey:
                      description: |-
                        TopologyKey is the key of node labels. Nodes that have a label with this key
                        and identical values are considered to be in the same topology.
                        We consider each <key, value> as a "bucket", and try to put balanced number
                        of pods into each bucket.
                        We define a domain as a particular instance of a topology.
                        Also, we define an eligible domain as a domain whose nodes meet the requirements of
                        nodeAffinityPolicy and nodeTaintsPolicy.
                        e.g. If TopologyKey is "kubernetes.io/hostname", each Node is a domain of that topology.
                        And, if TopologyKey is "topology.kubernetes.io/zone", each zone is a domain of that topology.
                        It's a required field.
                      type: string
                    whenUnsatisfiable:
                      description: |-
                        WhenUnsatisfiable indicates how to deal with a pod if it doesn't satisfy
                        the spread constraint.
                        - DoNotSchedule (default) tells the scheduler not to schedule it.
                        - ScheduleAnyway tells the scheduler to schedule the pod in any location,
                          but giving higher precedence to topologies that would help reduce the
                          skew.
                        A constraint is considered "Unsatisfiable" for an incoming pod
                        if and only if every possible node assignment for that pod would violate
                        "MaxSkew" on some topology.
                        For example, in a 3-zone cluster, MaxSkew is set to 1, and pods with the same
                        labelSelector spread as 3/1/1:
                        | zone1 | zone2 | zone3 |
                        | P P P |   P   |   P   |
                        If WhenUnsatisfiable is set to DoNotSchedule, incoming pod can only be scheduled
                        to zone2(zone3) to become 3/2/1(3/1/2) as ActualSkew(2-1) on zone2(zone3) satisfies
                        MaxSkew(1). In other words, the cluster can still be imbalanced, but scheduler
                        won't make it *more* imbalanced.
                        It's a required field.
                      type: string
                  required:
                  - maxSkew
                  - topologyKey
                  - whenUnsatisfiable
                  type: object
                type: array
            type: object
            x-kubernetes-validations:
            - message: shards is required when topology is Cluster
              rule: self.topology != 'Cluster' || has(self.shards)
            - message: shards is only valid when topology is Cluster
              rule: self.topology == 'Cluster' || !has(self.shards)
            - message: sentinel is required when topology is Sentinel
              rule: self.topology != 'Sentinel' || has(self.sentinel)
            - message: sentinel is only valid when topology is Sentinel
              rule: self.topology == 'Sentinel' || !has(self.sentinel)
            - message: replicateFrom only supported for Standalone or Replication
                topology
              rule: '!has(self.replicateFrom) || self.topology == ''Standalone'' ||
                self.topology == ''Replication'''
            - message: replicateFrom.caSecret requires replicateFrom.tls=true and
                spec.tls.enabled=true
              rule: '!has(self.replicateFrom) || !has(self.replicateFrom.caSecret)
                || (self.replicateFrom.tls && has(self.tls) && self.tls.enabled)'
            - message: perShardWorkload is only valid when topology is Cluster
              rule: '!has(self.perShardWorkload) || self.topology == ''Cluster'''
            - message: shards can only grow unless perShardWorkload=true (per-StatefulSet
                layout supports clean shard scale-down; single-STS removes replicas,
                not a shard — see ADR 0005 / C4)
              rule: '!has(self.shards) || !has(oldSelf.shards) || self.shards >= oldSelf.shards
                || (has(self.perShardWorkload) && self.perShardWorkload)'
            - message: perShardWorkload is immutable
              rule: has(self.perShardWorkload) == has(oldSelf.perShardWorkload) &&
                (!has(self.perShardWorkload) || self.perShardWorkload == oldSelf.perShardWorkload)
          status:
            description: ValkeyClusterStatus defines the observed state of ValkeyCluster.
            properties:
              clusterInitialized:
                description: |-
                  ClusterInitialized is true once `valkey-cli --cluster create` succeeded
                  for Cluster topology. The bootstrap is then never repeated.
                type: boolean
              conditions:
                description: |-
                  Conditions represent the current state of the resource.
                  Well-known types: Available, Progressing, Degraded, ClusterInitialized.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              internalEndpoint:
                description: |-
                  InternalEndpoint is the in-cluster client endpoint (host:port) of the
                  client Service, for consumers to connect to. Surfaced here so wrappers
                  (e.g. a Crossplane Composition) can project it without recomputing names.
                type: string
              lastAppliedReplicas:
                description: |-
                  LastAppliedReplicas is the total replica count last successfully
                  reflected in the running cluster (Cluster topology). Drives scale-up
                  detection: when totalReplicas(spec) > LastAppliedReplicas the operator
                  runs a one-shot add-node Job, then advances this counter.
                format: int32
                type: integer
              lastFailoverToken:
                description: |-
                  LastFailoverToken is the value of the valkey.wellcake.io/failover annotation
                  the operator last acted on (Replication topology). Used to run a manual
                  failover exactly once per request.
                type: string
              lastReshardToken:
                description: |-
                  LastReshardToken is the value of the valkey.wellcake.io/reshard annotation the
                  operator last acted on (Cluster topology). Used to run a manual reshard
                  exactly once per request.
                type: string
              observedGeneration:
                description: ObservedGeneration is the .metadata.generation last reconciled.
                format: int64
                type: integer
              phase:
                description: Phase is a quick high-level summary.
                enum:
                - Pending
                - Creating
                - Ready
                - Updating
                - Failed
                - Hibernated
                type: string
              primary:
                description: Primary holds the pod currently acting as primary (Replication/Sentinel
                  topologies).
                type: string
              readyReplicas:
                description: ReadyReplicas is the count of ready Valkey pods.
                format: int32
                type: integer
              readyShards:
                description: ReadyShards reports how many shards have a Ready primary
                  (Cluster topology only).
                format: int32
                type: integer
              shardDetails:
                description: |-
                  ShardDetails reports per-shard topology, slot ownership and health
                  (Cluster topology only). Refreshed by the reconciler on every pass
                  via `CLUSTER NODES`. Empty when topology != Cluster or the cluster
                  has not bootstrapped yet.
                items:
                  description: |-
                    ShardStatus describes a single shard (one primary plus its replicas) in a
                    Cluster-topology ValkeyCluster.
                  properties:
                    health:
                      description: |-
                        Health summarises the shard state: Ready, Degraded (primary up, some
                        replica down), Down (primary unreachable). Unknown when the survey
                        couldn't talk to any pod.
                      enum:
                      - Unknown
                      - Ready
                      - Degraded
                      - Down
                      type: string
                    index:
                      description: |-
                        Index is the operator-assigned shard ordinal, stable across reconciles.
                        It is the order in which we encountered the master in `CLUSTER NODES`,
                        not a Valkey-internal identifier.
                      format: int32
                      type: integer
                    maxLagBytes:
                      description: |-
                        MaxLagBytes is the largest (primaryOffset - replicaOffset) across the
                        shard's replicas. Equals 0 when there are no replicas or every replica
                        matches the primary exactly.
                      format: int64
                      type: integer
                    primary:
                      description: Primary is the pod name (e.g. "demo-3") that owns
                        this shard's slots.
                      type: string
                    primaryNodeID:
                      description: |-
                        PrimaryNodeID is the Valkey-internal cluster node ID of the primary.
                        Useful for `valkey-cli --cluster reshard --cluster-from <id>` operations.
                      type: string
                    primaryOffset:
                      description: |-
                        PrimaryOffset is the primary's master_repl_offset at the last survey,
                        reported by INFO replication. Useful as a freshness signal and as the
                        baseline for replica lag below.
                      format: int64
                      type: integer
                    replicaOffsets:
                      description: |-
                        ReplicaOffsets enumerates each replica's repl offset alongside the
                        primary's, so lag can be calculated per replica.
                      items:
                        description: ReplicaOffset records one replica's repl offset
                          alongside its pod name.
                        properties:
                          offset:
                            format: int64
                            type: integer
                          pod:
                            type: string
                        required:
                        - offset
                        - pod
                        type: object
                      type: array
                    replicas:
                      description: Replicas lists pod names that replicate this primary.
                      items:
                        type: string
                      type: array
                    slotCount:
                      description: SlotCount is the total slot count owned by this
                        shard.
                      format: int32
                      type: integer
                    slotRanges:
                      description: |-
                        SlotRanges enumerates the slot ranges owned by the primary, e.g.
                        ["0-5460", "12000-12100"]. A shard with zero slots (freshly added by
                        scale-up but not yet reshard'ed) shows an empty list.
                      items:
                        type: string
                      type: array
                  required:
                  - index
                  - primary
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - index
                x-kubernetes-list-type: map
              shards:
                description: Shards reports the configured number of shards (Cluster
                  topology only).
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .spec.topology
      name: Topology
      type: string
    - jsonPath: .spec.replicas
      name: Replicas
      type: integer
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ValkeyCluster is the Schema for the valkeyclusters API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ValkeyClusterSpec defines the desired state of ValkeyCluster.
            properties:
              affinity:
                description: Affinity for Valkey pods. If nil the operator applies
                  pod anti-affinity by default.
                properties:
                  nodeAffinity:
                    description: Describes node affinity scheduling rules for the
                      pod.
                    properties:
                      preferredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          The scheduler will prefer to schedule pods to nodes that satisfy
                          the affinity expressions specified by this field, but it may choose
                          a node that violates one or more of the expressions. The node that is
                          most preferred is the one with the greatest sum of weights, i.e.
                          for each node that meets all of the scheduling requirements (resource
                          request, requiredDuringScheduling affinity expressions, etc.),
                          compute a sum by iterating through the elements of this field and adding
                          "weight" to the sum if the node matches the corresponding matchExpressions; the
                          node(s) with the highest sum are the most preferred.
                        items:
                          description: |-
                            An empty preferred scheduling term matches all objects with implicit weight 0
                            (i.e. it's a no-op). A null preferred scheduling term matches no objects (i.e. is also a no-op).
                          properties:
                            preference:
                              description: A node selector term, associated with the
                                corresponding weight.
                              properties:
                                matchExpressions:
                                  description: A list of node selector requirements
                                    by node's labels.
                                  items:
                                    description: |-
                                      A node selector requirement is a selector that contains values, a key, and an operator
                                      that relates the key and values.
                                    properties:
                                      key:
                                        description: The label key that the selector
                                          applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          Represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                        type: string
                                      values:
                                        description: |-
                                          An array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. If the operator is Gt or Lt, the values
                                          array must have a single element, which will be interpreted as an integer.
                                          This array is replaced during a strategic merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchFields:
                                  description: A list of node selector requirements
                                    by node's fields.
                                  items:
                                    description: |-
                                      A node selector requirement is a selector that contains values, a key, and an operator
                                      that relates the key and values.
                                    properties:
                                      key:
                                        description: The label key that the selector
                                          applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          Represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                        type: string
                                      values:
                                        description: |-
                                          An array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. If the operator is Gt or Lt, the values
                                          array must have a single element, which will be interpreted as an integer.
                                          This array is replaced during a strategic merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                              type: object
                              x-kubernetes-map-type: atomic
                            weight:
                              description: Weight associated with matching the corresponding
                                nodeSelectorTerm, in the range 1-100.
                              format: int32
                              type: integer
                          required:
                          - preference
                          - weight
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      requiredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          If the affinity requirements specified by this field are not met at
                          scheduling time, the pod will not be scheduled onto the node.
                          If the affinity requirements specified by this field cease to be met
                          at some point during pod execution (e.g. due to an update), the system
                          may or may not try to eventually evict the pod from its node.
                        properties:
                          nodeSelectorTerms:
                            description: Required. A list of node selector terms.
                              The terms are ORed.
                            items:
                              description: |-
                                A null or empty node selector term matches no objects. The requirements of
                                them are ANDed.
                                The TopologySelectorTerm type implements a subset of the NodeSelectorTerm.
                              properties:
                                matchExpressions:
                                  description: A list of node selector requirements
                                    by node's labels.
                                  items:
                                    description: |-
                                      A node selector requirement is a selector that contains values, a key, and an operator
                                      that relates the key and values.
                                    properties:
                                      key:
                                        description: The label key that the selector
                                          applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          Represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                        type: string
                                      values:
                                        description: |-
                                          An array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. If the operator is Gt or Lt, the values
                                          array must have a single element, which will be interpreted as an integer.
                                          This array is replaced during a strategic merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchFields:
                                  description: A list of node selector requirements
                                    by node's fields.
                                  items:
                                    description: |-
                                      A node selector requirement is a selector that contains values, a key, and an operator
                                      that relates the key and values.
                                    properties:
                                      key:
                                        description: The label key that the selector
                                          applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          Represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                        type: string
                                      values:
                                        description: |-
                                          An array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. If the operator is Gt or Lt, the values
                                          array must have a single element, which will be interpreted as an integer.
                                          This array is replaced during a strategic merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                              type: object
                              x-kubernetes-map-type: atomic
                            type: array
                            x-kubernetes-list-type: atomic
                        required:
                        - nodeSelectorTerms
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  podAffinity:
                    description: Describes pod affinity scheduling rules (e.g. co-locate
                      this pod in the same node, zone, etc. as some other pod(s)).
                    properties:
                      preferredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          The scheduler will prefer to schedule pods to nodes that satisfy
                          the affinity expressions specified by this field, but it may choose
                          a node that violates one or more of the expressions. The node that is
                          most preferred is the one with the greatest sum of weights, i.e.
                          for each node that meets all of the scheduling requirements (resource
                          request, requiredDuringScheduling affinity expressions, etc.),
                          compute a sum by iterating through the elements of this field and adding
                          "weight" to the sum if the node has pods which matches the corresponding podAffinityTerm; the
                          node(s) with the highest sum are the most preferred.
                        items:
                          description: The weights of all of the matched WeightedPodAffinityTerm
                            fields are added per-node to find the most preferred node(s)
                          properties:
                            podAffinityTerm:
                              description: Required. A pod affinity term, associated
                                with the corresponding weight.
                              properties:
                                labelSelector:
                                  description: |-
                                    A label query over a set of resources, in this case pods.
                                    If it's null, this PodAffinityTerm matches with no Pods.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                matchLabelKeys:
                                  description: |-
                                    MatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                    Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                mismatchLabelKeys:
                                  description: |-
                                    MismatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                    Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                namespaceSelector:
                                  description: |-
                                    A label query over the set of namespaces that the term applies to.
                                    The term is applied to the union of the namespaces selected by this field
                                    and the ones listed in the namespaces field.
                                    null selector and null or empty namespaces list means "this pod's namespace".
                                    An empty selector ({}) matches all namespaces.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: |-
                                    namespaces specifies a static list of namespace names that the term applies to.
                                    The term is applied to the union of the namespaces listed in this field
                                    and the ones selected by namespaceSelector.
                                    null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                topologyKey:
                                  description: |-
                                    This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                    the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                    whose value of the label with key topologyKey matches that of any node on which any of the
                                    selected pods is running.
                                    Empty topologyKey is not allowed.
                                  type: string
                              required:
                              - topologyKey
                              type: object
                            weight:
                              description: |-
                                weight associated with matching the corresponding podAffinityTerm,
                                in the range 1-100.
                              format: int32
                              type: integer
                          required:
                          - podAffinityTerm
                          - weight
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      requiredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          If the affinity requirements specified by this field are not met at
                          scheduling time, the pod will not be scheduled onto the node.
                          If the affinity requirements specified by this field cease to be met
                          at some point during pod execution (e.g. due to a pod label update), the
                          system may or may not try to eventually evict the pod from its node.
                          When there are multiple elements, the lists of nodes corresponding to each
                          podAffinityTerm are intersected, i.e. all terms must be satisfied.
                        items:
                          description: |-
                            Defines a set of pods (namely those matching the labelSelector
                            relative to the given namespace(s)) that this pod should be
                            co-located (affinity) or not co-located (anti-affinity) with,
                            where co-located is defined as running on a node whose value of
                            the label with key <topologyKey> matches that of any node on which
                            a pod of the set of pods is running
                          properties:
                            labelSelector:
                              description: |-
                                A label query over a set of resources, in this case pods.
                                If it's null, this PodAffinityTerm matches with no Pods.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            matchLabelKeys:
                              description: |-
                                MatchLabelKeys is a set of pod label keys to select which pods will
                                be taken into consideration. The keys are used to lookup values from the
                                incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                to select the group of existing pods which pods will be taken into consideration
                                for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                pod labels will be ignored. The default value is empty.
                                The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                Also, matchLabelKeys cannot be set when labelSelector isn't set.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            mismatchLabelKeys:
                              description: |-
                                MismatchLabelKeys is a set of pod label keys to select which pods will
                                be taken into consideration. The keys are used to lookup values from the
                                incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                to select the group of existing pods which pods will be taken into consideration
                                for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                pod labels will be ignored. The default value is empty.
                                The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            namespaceSelector:
                              description: |-
                                A label query over the set of namespaces that the term applies to.
                                The term is applied to the union of the namespaces selected by this field
                                and the ones listed in the namespaces field.
                                null selector and null or empty namespaces list means "this pod's namespace".
                                An empty selector ({}) matches all namespaces.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            namespaces:
                              description: |-
                                namespaces specifies a static list of namespace names that the term applies to.
                                The term is applied to the union of the namespaces listed in this field
                                and the ones selected by namespaceSelector.
                                null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            topologyKey:
                              description: |-
                                This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                whose value of the label with key topologyKey matches that of any node on which any of the
                                selected pods is running.
                                Empty topologyKey is not allowed.
                              type: string
                          required:
                          - topologyKey
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                    type: object
                  podAntiAffinity:
                    description: Describes pod anti-affinity scheduling rules (e.g.
                      avoid putting this pod in the same node, zone, etc. as some
                      other pod(s)).
                    properties:
                      preferredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          The scheduler will prefer to schedule pods to nodes that satisfy
                          the anti-affinity expressions specified by this field, but it may choose
                          a node that violates one or more of the expressions. The node that is
                          most preferred is the one with the greatest sum of weights, i.e.
                          for each node that meets all of the scheduling requirements (resource
                          request, requiredDuringScheduling anti-affinity expressions, etc.),
                          compute a sum by iterating through the elements of this field and subtracting
                          "weight" from the sum if the node has pods which matches the corresponding podAffinityTerm; the
                          node(s) with the highest sum are the most preferred.
                        items:
                          description: The weights of all of the matched WeightedPodAffinityTerm
                            fields are added per-node to find the most preferred node(s)
                          properties:
                            podAffinityTerm:
                              description: Required. A pod affinity term, associated
                                with the corresponding weight.
                              properties:
                                labelSelector:
                                  description: |-
                                    A label query over a set of resources, in this case pods.
                                    If it's null, this PodAffinityTerm matches with no Pods.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                matchLabelKeys:
                                  description: |-
                                    MatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                    Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                mismatchLabelKeys:
                                  description: |-
                                    MismatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                    Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                namespaceSelector:
                                  description: |-
                                    A label query over the set of namespaces that the term applies to.
                                    The term is applied to the union of the namespaces selected by this field
                                    and the ones listed in the namespaces field.
                                    null selector and null or empty namespaces list means "this pod's namespace".
                                    An empty selector ({}) matches all namespaces.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: |-
                                    namespaces specifies a static list of namespace names that the term applies to.
                                    The term is applied to the union of the namespaces listed in this field
                                    and the ones selected by namespaceSelector.
                                    null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                topologyKey:
                                  description: |-
                                    This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                    the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                    whose value of the label with key topologyKey matches that of any node on which any of the
                                    selected pods is running.
                                    Empty topologyKey is not allowed.
                                  type: string
                              required:
                              - topologyKey
                              type: object
                            weight:
                              description: |-
                                weight associated with matching the corresponding podAffinityTerm,
                                in the range 1-100.
                              format: int32
                              type: integer
                          required:
                          - podAffinityTerm
                          - weight
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      requiredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          If the anti-affinity requirements specified by this field are not met at
                          scheduling time, the pod will not be scheduled onto the node.
                          If the anti-affinity requirements specified by this field cease to be met
                          at some point during pod execution (e.g. due to a pod label update), the
                          system may or may not try to eventually evict the pod from its node.
                          When there are multiple elements, the lists of nodes corresponding to each
                          podAffinityTerm are intersected, i.e. all terms must be satisfied.
                        items:
                          description: |-
                            Defines a set of pods (namely those matching the labelSelector
                            relative to the given namespace(s)) that this pod should be
                            co-located (affinity) or not co-located (anti-affinity) with,
                            where co-located is defined as running on a node whose value of
                            the label with key <topologyKey> matches that of any node on which
                            a pod of the set of pods is running
                          properties:
                            labelSelector:
                              description: |-
                                A label query over a set of resources, in this case pods.
                                If it's null, this PodAffinityTerm matches with no Pods.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            matchLabelKeys:
                              description: |-
                                MatchLabelKeys is a set of pod label keys to select which pods will
                                be taken into consideration. The keys are used to lookup values from the
                                incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                to select the group of existing pods which pods will be taken into consideration
                                for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                pod labels will be ignored. The default value is empty.
                                The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                Also, matchLabelKeys cannot be set when labelSelector isn't set.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            mismatchLabelKeys:
                              description: |-
                                MismatchLabelKeys is a set of pod label keys to select which pods will
                                be taken into consideration. The keys are used to lookup values from the
                                incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                to select the group of existing pods which pods will be taken into consideration
                                for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                pod labels will be ignored. The default value is empty.
                                The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            namespaceSelector:
                              description: |-
                                A label query over the set of namespaces that the term applies to.
                                The term is applied to the union of the namespaces selected by this field
                                and the ones listed in the namespaces field.
                                null selector and null or empty namespaces list means "this pod's namespace".
                                An empty selector ({}) matches all namespaces.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            namespaces:
                              description: |-
                                namespaces specifies a static list of namespace names that the term applies to.
                                The term is applied to the union of the namespaces listed in this field
                                and the ones selected by namespaceSelector.
                                null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            topologyKey:
                              description: |-
                                This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                whose value of the label with key topologyKey matches that of any node on which any of the
                                selected pods is running.
                                Empty topologyKey is not allowed.
                              type: string
                          required:
                          - topologyKey
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                    type: object
                type: object
              auth:
                description: Auth configures password authentication.
                properties:
                  enabled:
                    default: true
                    description: Enabled toggles requirepass.
                    type: boolean
                  existingSecret:
                    description: |-
                      ExistingSecret holds the password. If empty, the operator generates one.
                      The secret must contain key "password".
                    type: string
                type: object
              autoReshard:
                default: true
                description: |-
                  AutoReshard, when true and topology=Cluster, makes scale-up runs also
                  invoke `valkey-cli --cluster rebalance --cluster-use-empty-masters` so
                  new shards receive their share of slots automatically, and makes
                  scale-down rebalance the surviving masters after slots are migrated off
                  the leaving nodes. Defaults to true so a Crossplane/GitOps-driven change
                  to `shards` converges to a balanced, fully-covered cluster with no manual
                  `valkey-cli --cluster reshard`. Set false to keep slot (re)distribution a
                  deliberate manual step.
                type: boolean
              backup:
                description: Backup configures periodic RDB snapshot upload to S3-compatible
                  storage.
                properties:
                  awsCLIImage:
                    default: amazon/aws-cli:2.15.0
                    description: AWSCLIImage override for the upload step.
                    type: string
                  enabled:
                    description: Enabled toggles the CronJob.
                    type: boolean
                  image:
                    description: |-
                      Image override for the dump step. Defaults to spec.image (Valkey image
                      includes valkey-cli).
                    type: string
                  retention:
                    default: 7
                    description: |-
                      Retention is the number of most-recent snapshots to keep under the S3
                      prefix. Older snapshots matching `<prefix><cluster>-*.rdb` are deleted
                      after each successful upload. 0 disables retention (keep forever).
                    format: int32
                    minimum: 0
                    type: integer
                  s3:
                    description: S3 holds destination details.
                    properties:
                      bucket:
                        description: Bucket name.
                        minLength: 1
                        type: string
                      credentialsSecret:
                        description: |-
                          CredentialsSecret references a Secret with keys
                          AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY.
                        minLength: 1
                        type: string
                      encryption:
                        description: |-
                          Encryption selects server-side encryption for uploads.
                            AES256 — SSE-S3, AWS-managed keys (no extra setup, no extra cost)
                            KMS    — SSE-KMS, customer-controlled key in KMSKeyID (translated to
                                     "aws:kms" when invoking the AWS CLI).
                          Empty disables SSE on upload. Reading SSE-S3 objects is transparent;
                          reading SSE-KMS objects requires the same KMS permissions on the
                          reading principal (so the restore Secret must have kms:Decrypt on the
                          key).
                        enum:
                        - AES256
                        - KMS
                        type: string
                      endpoint:
                        description: Endpoint URL for S3-compatible services. Leave
                          empty for AWS S3.
                        type: string
                      kmsKeyId:
                        description: KMSKeyID is the KMS key ARN/ID/alias when Encryption=aws:kms.
                        type: string
                      prefix:
                        description: |-
                          Prefix is prepended to the object key. The final key is
                            <prefix>/<cluster-name>-YYYYMMDD-HHMMSS.rdb
                        type: string
                      region:
                        default: us-east-1
                        description: Region, defaults to "us-east-1" for AWS / empty
                          for many MinIO setups.
                        type: string
                    required:
                    - bucket
                    - credentialsSecret
                    type: object
                  schedule:
                    default: 0 3 * * *
                    description: Schedule is a standard cron expression, e.g. "0 3
                      * * *".
                    type: string
                required:
                - s3
                type: object
              config:
                additionalProperties:
                  type: string
                description: Config is a free-form map of Valkey config directives
                  appended to valkey.conf.
                type: object
              image:
                default: valkey/valkey:8.0
                description: Image is the Valkey container image. Defaults to a known
                  stable tag.
                type: string
              imagePullPolicy:
                default: IfNotPresent
                description: ImagePullPolicy for the Valkey container.
                type: string
              metrics:
                description: Metrics enables the Prometheus exporter sidecar.
                properties:
                  enabled:
                    description: Enabled toggles the exporter sidecar.
                    type: boolean
                  image:
                    default: oliver006/redis_exporter:v1.62.0
                    description: Image of the exporter.
                    type: string
                  serviceMonitor:
                    description: ServiceMonitor creates a Prometheus Operator ServiceMonitor.
                    type: boolean
                type: object
              networkPolicy:
                description: NetworkPolicy generates a default-deny + allow-from policy
                  if Enabled.
                properties:
                  allowFrom:
                    description: |-
                      AllowFrom is a list of pod/namespace selectors allowed to reach Valkey ports.
                      If empty, only pods in the same namespace are allowed.
                    items:
                      description: |-
                        NetworkPolicyPeer describes a peer to allow traffic to/from. Only certain combinations of
                        fields are allowed
                      properties:
                        ipBlock:
                          description: |-
                            ipBlock defines policy on a particular IPBlock. If this field is set then
                            neither of the other fields can be.
                          properties:
                            cidr:
                              description: |-
                                cidr is a string representing the IPBlock
                                Valid examples are "192.168.1.0/24" or "2001:db8::/64"
                              type: string
                            except:
                              description: |-
                                except is a slice of CIDRs that should not be included within an IPBlock
                                Valid examples are "192.168.1.0/24" or "2001:db8::/64"
                                Except values will be rejected if they are outside the cidr range
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - cidr
                          type: object
                        namespaceSelector:
                          description: |-
                            namespaceSelector selects namespaces using cluster-scoped labels. This field follows
                            standard label selector semantics; if present but empty, it selects all namespaces.

                            If podSelector is also set, then the NetworkPolicyPeer as a whole selects
                            the pods matching podSelector in the namespaces selected by namespaceSelector.
                            Otherwise it selects all pods in the namespaces selected by namespaceSelector.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        podSelector:
                          description: |-
                            podSelector is a label selector which selects pods. This field follows standard label
                            selector semantics; if present but empty, it selects all pods.

                            If namespaceSelector is also set, then the NetworkPolicyPeer as a whole selects
                            the pods matching podSelector in the Namespaces selected by NamespaceSelector.
                            Otherwise it selects the pods matching podSelector in the policy's own namespace.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                    type: array
                  enabled:
                    description: Enabled toggles NetworkPolicy generation. Defaults
                      to false (opt-in for now).
                    type: boolean
                type: object
              nodeSelector:
                additionalProperties:
                  type: string
                description: NodeSelector for Valkey pods.
                type: object
              perShardWorkload:
                description: |-
                  PerShardWorkload, when true (Cluster topology only), renders ONE
                  StatefulSet per shard instead of a single StatefulSet for the whole
                  cluster (ADR 0005). This unlocks shard-aware anti-affinity (a shard's
                  primary+replicas on different nodes — not expressible from one shared pod
                  template) and clean shard scale-down (remove a shard = delete its STS).
                  Immutable (see the spec-level rule): pod identity and bootstrap differ
                  fundamentally, so switching requires recreating the cluster (blue-green via
                  restore). EXPERIMENTAL — being built incrementally behind this opt-in;
                  default (absent → false) keeps the single-StatefulSet path.
                type: boolean
              podDisruptionBudget:
                description: |-
                  PodDisruptionBudget controls voluntary disruptions for the Valkey StatefulSet.
                  Defaults to MaxUnavailable=1 when nil.
                properties:
                  enabled:
                    default: true
                    description: Enabled toggles the PDB. Defaults to true.
                    type: boolean
                  maxUnavailable:
                    anyOf:
                    - type: integer
                    - type: string
                    description: MaxUnavailable, mutually exclusive with MinAvailable.
                      Default "1".
                    x-kubernetes-int-or-string: true
                  minAvailable:
                    anyOf:
                    - type: integer
                    - type: string
                    description: MinAvailable, mutually exclusive with MaxUnavailable.
                    x-kubernetes-int-or-string: true
                type: object
              profile:
                default: Cache
                description: |-
                  Profile selects the workload profile (Cache or Durable). Immutable —
                  changing profile would require re-sizing PVCs and flipping persistence,
                  which is not safe in-place. Drives defaults for persistence, eviction
                  policy and maxmemory headroom.
                enum:
                - Cache
                - Durable
                type: string
                x-kubernetes-validations:
                - message: profile is immutable
                  rule: self == oldSelf
              replicas:
                default: 3
                description: |-
                  Replicas is the number of Valkey instances for Standalone/Replication
                  (1 primary + Replicas-1 replicas). IGNORED for Cluster topology — there the
                  node count is derived from Shards*(1+ReplicasPerShard); set those instead.
                format: int32
                minimum: 1
                type: integer
              replicasPerShard:
                description: ReplicasPerShard for Cluster topology — replicas per
                  primary.
                format: int32
                minimum: 0
                type: integer
              replicateFrom:
                description: |-
                  ReplicateFrom makes this cluster a pull-based asynchronous replica of
                  an external Valkey/Redis primary, usually in another region or another
                  Kubernetes cluster — a DR / read-scaling pattern.

                  When set, every pod (including pod-0) runs `replicaof <host> <port>`
                  at startup, and operator-driven local failover is disabled (the local
                  pods are all replicas of an external source). Promotion on DR is a
                  manual step: clear spec.replicateFrom, the operator then lets pod-0
                  resume primary role and replicas attach to it.

                  Supported topologies: Standalone, Replication.
                  Sentinel and Cluster are unsupported: Sentinel would race the local
                  quorum against the external primary, and OSS Valkey Cluster has no
                  cross-cluster replication semantics.
                properties:
                  caSecret:
                    description: |-
                      CASecret references a Secret holding the source cluster's CA bundle
                      (PEM), used to verify the upstream primary's TLS certificate when the
                      source is signed by a different CA than this cluster — the common
                      multi-region / cross-Kubernetes case (S4).

                      Valkey reads a single global tls-ca-cert-file for all TLS connections, so
                      the operator merges this CA with this cluster's own CA into one combined
                      trust bundle that tls-ca-cert-file points at — no manual volume mount. The
                      referenced Secret must live in the same namespace as the ValkeyCluster;
                      its key defaults to "ca.crt".

                      Only meaningful when tls=true above and spec.tls.enabled=true (the local
                      pods need their own TLS cert/key to speak tls-replication outbound).
                      Source-CA rotation is picked up on the next pod restart (the bundle is
                      built at pod init).
                    properties:
                      key:
                        default: ca.crt
                        description: Key in the Secret whose value is the PEM CA bundle.
                          Defaults to "ca.crt".
                        type: string
                      name:
                        description: Name of the Secret holding the source cluster's
                          CA bundle.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  host:
                    description: |-
                      Host is the external primary FQDN or IP — typically a LoadBalancer
                      service, a Sentinel-discovered address, or a peered ClusterIP.
                    minLength: 1
                    type: string
                  passwordSecret:
                    description: |-
                      PasswordSecret references the source cluster's auth password,
                      used as masterauth on this cluster. If unset, replication is
                      attempted without auth.
                    properties:
                      key:
                        default: password
                        type: string
                      name:
                        type: string
                    required:
                    - name
                    type: object
                  port:
                    default: 6379
                    description: Port of the external primary. Defaults to 6379.
                    format: int32
                    type: integer
                  tls:
                    description: |-
                      TLS indicates the upstream primary accepts TLS connections. When true,
                      replication frames are encrypted (`tls-replication yes`). The upstream
                      certificate must chain to a CA this cluster trusts. In a multi-region
                      setup where the source has its own CA (different from this cluster's),
                      set caSecret below — the operator then trusts both. If source and target
                      share a CA, leave caSecret empty.
                    type: boolean
                required:
                - host
                type: object
              resources:
                description: Resources for the Valkey container.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              restoreFrom:
                description: |-
                  RestoreFrom downloads an RDB from S3 into /data/dump.rdb on each pod
                  before Valkey starts, but ONLY when /data/dump.rdb does not already
                  exist. This makes restore a no-op once a cluster has been bootstrapped:
                  to re-restore, delete /data/dump.rdb via a debug pod first.

                  Standalone/Replication/Sentinel: sourceKey is used verbatim on every pod;
                  replicas resync from the primary after it loads the RDB.

                  Cluster: restore is per-shard — sourceKey must contain a "{shard}"
                  placeholder; each master ordinal pulls its own snapshot. The operator does
                  NOT auto-run `valkey-cli --cluster create` in this case (it aborts on
                  non-empty nodes); cluster assembly is a guided manual procedure
                  (docs/runbook.md#cluster-restore), after which the operator detects the
                  formed cluster and marks it initialized.
                properties:
                  awsCLIImage:
                    default: amazon/aws-cli:2.15.0
                    description: AWSCLIImage override for the download init container.
                    type: string
                  s3:
                    description: |-
                      S3 destination details. Reuses the same shape as backup.s3, including
                      the credentials Secret. The credentialsSecret may be the same Secret
                      used by Backup, or a separate read-only one.
                    properties:
                      bucket:
                        description: Bucket name.
                        minLength: 1
                        type: string
                      credentialsSecret:
                        description: |-
                          CredentialsSecret references a Secret with keys
                          AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY.
                        minLength: 1
                        type: string
                      encryption:
                        description: |-
                          Encryption selects server-side encryption for uploads.
                            AES256 — SSE-S3, AWS-managed keys (no extra setup, no extra cost)
                            KMS    — SSE-KMS, customer-controlled key in KMSKeyID (translated to
                                     "aws:kms" when invoking the AWS CLI).
                          Empty disables SSE on upload. Reading SSE-S3 objects is transparent;
                          reading SSE-KMS objects requires the same KMS permissions on the
                          reading principal (so the restore Secret must have kms:Decrypt on the
                          key).
                        enum:
                        - AES256
                        - KMS
                        type: string
                      endpoint:
                        description: Endpoint URL for S3-compatible services. Leave
                          empty for AWS S3.
                        type: string
                      kmsKeyId:
                        description: KMSKeyID is the KMS key ARN/ID/alias when Encryption=aws:kms.
                        type: string
                      prefix:
                        description: |-
                          Prefix is prepended to the object key. The final key is
                            <prefix>/<cluster-name>-YYYYMMDD-HHMMSS.rdb
                        type: string
                      region:
                        default: us-east-1
                        description: Region, defaults to "us-east-1" for AWS / empty
                          for many MinIO setups.
                        type: string
                    required:
                    - bucket
                    - credentialsSecret
                    type: object
                  sourceKey:
                    description: |-
                      SourceKey is the S3 key to download.

                      Single-shard topologies (Standalone/Replication/Sentinel) use it verbatim,
                      e.g. "backups/web-cache-20260528-030000.rdb".

                      For Cluster topology the restore is per-shard: SourceKey must contain a
                      "{shard}" placeholder which each master ordinal (0..shards-1) substitutes
                      with its index, e.g. "backups/web-20260528-030000-shard-{shard}.rdb".
                      Cluster restore requires the same shard count as the snapshot and is a
                      guided manual procedure — see docs/runbook.md#cluster-restore.
                    minLength: 1
                    type: string
                required:
                - s3
                - sourceKey
                type: object
              sentinel:
                description: Sentinel-specific settings (only used when Topology=Sentinel).
                properties:
                  image:
                    description: Image of the Sentinel container. Defaults to the
                      same image as Valkey.
                    type: string
                  quorum:
                    description: Quorum override. If zero, computed as Replicas/2
                      + 1.
                    format: int32
                    type: integer
                  replicas:
                    default: 3
                    description: Replicas is the number of Sentinel pods. Quorum is
                      Replicas/2 + 1.
                    format: int32
                    minimum: 3
                    type: integer
                type: object
              shards:
                description: Shards is only used for Cluster topology. Number of shards
                  (primaries).
                format: int32
                minimum: 3
                type: integer
              storage:
                description: Storage configures persistence backed by PVCs.
                properties:
                  medium:
                    default: Disk
                    description: |-
                      Medium selects what backs the data dir. "Disk" (default) provisions a PVC.
                      "Memory" backs it with a tmpfs emptyDir (RAM): no PVC, no StorageClass, no
                      PV-bind latency, and — crucially — it counts against the pod's memory limit
                      rather than ephemeral-storage, so it does NOT trip the nodefs eviction that
                      made plain (disk-backed) emptyDir unsafe under ephemeral-storage pressure. Intended for the Cache
                      profile (ephemeral data); data does NOT survive a pod restart, so it is
                      rejected for the Durable profile. Immutable.
                    enum:
                    - Disk
                    - Memory
                    type: string
                    x-kubernetes-validations:
                    - message: storage medium is immutable
                      rule: self == oldSelf
                  mode:
                    default: rdb
                    description: |-
                      Persistence mode: rdb, aof, both, none. Immutable: switching the
                      persistence mode on a live cluster is unsafe — e.g. rdb→none leaves a
                      stale RDB on the PVC that a later restart would reload, and none→aof
                      starts an append-only file with no initial snapshot. Recreate the cluster
                      to change it. (Design-review Q6/SC4.)
                    enum:
                    - rdb
                    - aof
                    - both
                    - none
                    type: string
                    x-kubernetes-validations:
                    - message: storage mode is immutable
                      rule: self == oldSelf
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 10Gi
                    description: |-
                      Size of each PVC, e.g. "10Gi". Expand-only: the value may grow but never
                      shrink (PVC expansion in k8s only works upwards anyway, and only if the
                      StorageClass has AllowVolumeExpansion=true).
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                    x-kubernetes-validations:
                    - message: storage size cannot shrink
                      rule: quantity(self).compareTo(quantity(oldSelf)) >= 0
                  storageClassName:
                    description: |-
                      StorageClassName for the PVC. Immutable: changing the StorageClass on an
                      existing StatefulSet has no effect on already-bound PVCs and can lead to
                      inconsistent storage between pod ordinals.
                    type: string
                    x-kubernetes-validations:
                    - message: storageClassName is immutable
                      rule: self == oldSelf
                type: object
              tls:
                description: TLS configures TLS via cert-manager.
                properties:
                  enabled:
                    description: Enabled toggles TLS for client and replication traffic.
                    type: boolean
                  existingSecret:
                    description: |-
                      ExistingSecret is a pre-created secret containing tls.crt, tls.key, ca.crt.
                      Either IssuerRef or ExistingSecret must be set when Enabled=true.
                    type: string
                  issuerRef:
                    description: IssuerRef points at a cert-manager Issuer or ClusterIssuer.
                    properties:
                      group:
                        type: string
                      kind:
                        default: Issuer
                        enum:
                        - Issuer
                        - ClusterIssuer
                        type: string
                      name:
                        type: string
                    required:
                    - name
                    type: object
                  mutualTLS:
                    description: |-
                      MutualTLS, when true, requires clients to present a certificate signed by
                      the cluster CA (`tls-auth-clients yes`). Default false keeps client certs
                      optional (`tls-auth-clients optional`) — pod-to-pod replication always
                      uses the mounted cert regardless. Set true to enforce mTLS, as the
                      platform DBaaS contract does.
                    type: boolean
                type: object
              tolerations:
                description: Tolerations for Valkey pods.
                items:
                  description: |-
                    The pod this Toleration is attached to tolerates any taint that matches
                    the triple <key,value,effect> using the matching operator <operator>.
                  properties:
                    effect:
                      description: |-
                        Effect indicates the taint effect to match. Empty means match all taint effects.
                        When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                      type: string
                    key:
                      description: |-
                        Key is the taint key that the toleration applies to. Empty means match all taint keys.
                        If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                      type: string
                    operator:
                      description: |-
                        Operator represents a key's relationship to the value.
                        Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                        Exists is equivalent to wildcard for value, so that a pod can
                        tolerate all taints of a particular category.
                        Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                      type: string
                    tolerationSeconds:
                      description: |-
                        TolerationSeconds represents the period of time the toleration (which must be
                        of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                        it is not set, which means tolerate the taint forever (do not evict). Zero and
                        negative values will be treated as 0 (evict immediately) by the system.
                      format: int64
                      type: integer
                    value:
                      description: |-
                        Value is the taint value the toleration matches to.
                        If the operator is Exists, the value should be empty, otherwise just a regular string.
                      type: string
                  type: object
                type: array
              topology:
                default: Replication
                description: |-
                  Topology selects the deployment shape. Immutable after creation —
                  switching shape requires a new resource because PVCs, layout and
                  bootstrap differ fundamentally.
                enum:
                - Standalone
                - Replication
                - Sentinel
                - Cluster
                type: string
                x-kubernetes-validations:
                - message: topology is immutable
                  rule: self == oldSelf
              topologySpreadConstraints:
                description: |-
                  TopologySpreadConstraints to spread pods across zones/DCs.
                  If nil the operator applies a default hostname spread across availability zones.
                items:
                  description: TopologySpreadConstraint specifies how to spread matching
                    pods among the given topology.
                  properties:
                    labelSelector:
                      description: |-
                        LabelSelector is used to find matching pods.
                        Pods that match this label selector are counted to determine the number of pods
                        in their corresponding topology domain.
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                    matchLabelKeys:
                      description: |-
                        MatchLabelKeys is a set of pod label keys to select the pods over which
                        spreading will be calculated. The keys are used to lookup values from the
                        incoming pod labels, those key-value labels are ANDed with labelSelector
                        to select the group of existing pods over which spreading will be calculated
                        for the incoming pod. The same key is forbidden to exist in both MatchLabelKeys and LabelSelector.
                        MatchLabelKeys cannot be set when LabelSelector isn't set.
                        Keys that don't exist in the incoming pod labels will
                        be ignored. A null or empty list means only match against labelSelector.

                        This is a beta field and requires the MatchLabelKeysInPodTopologySpread feature gate to be enabled (enabled by default).
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                    maxSkew:
                      description: |-
                        MaxSkew describes the degree to which pods may be unevenly distributed.
                        When `whenUnsatisfiable=DoNotSchedule`, it is the maximum permitted difference
                        between the number of matching pods in the target topology and the global minimum.
                        The global minimum is the minimum number of matching pods in an eligible domain
                        or zero if the number of eligible domains is less than MinDomains.
                        For example, in a 3-zone cluster, MaxSkew is set to 1, and pods with the same
                        labelSelector spread as 2/2/1:
                        In this case, the global minimum is 1.
                        | zone1 | zone2 | zone3 |
                        |  P P  |  P P  |   P   |
                        - if MaxSkew is 1, incoming pod can only be scheduled to zone3 to become 2/2/2;
                        scheduling it onto zone1(zone2) would make the ActualSkew(3-1) on zone1(zone2)
                        violate MaxSkew(1).
                        - if MaxSkew is 2, incoming pod can be scheduled onto any zone.
                        When `whenUnsatisfiable=ScheduleAnyway`, it is used to give higher precedence
                        to topologies that satisfy it.
                        It's a required field. Default value is 1 and 0 is not allowed.
                      format: int32
                      type: integer
                    minDomains:
                      description: |-
                        MinDomains indicates a minimum number of eligible domains.
                        When the number of eligible domains with matching topology keys is less than minDomains,
                        Pod Topology Spread treats "global minimum" as 0, and then the calculation of Skew is performed.
                        And when the number of eligible domains with matching topology keys equals or greater than minDomains,
                        this value has no effect on scheduling.
                        As a result, when the number of eligible domains is less than minDomains,
                        scheduler won't schedule more than maxSkew Pods to those domains.
                        If value is nil, the constraint behaves as if MinDomains is equal to 1.
                        Valid values are integers greater than 0.
                        When value is not nil, WhenUnsatisfiable must be DoNotSchedule.

                        For example, in a 3-zone cluster, MaxSkew is set to 2, MinDomains is set to 5 and pods with the same
                        labelSelector spread as 2/2/2:
                        | zone1 | zone2 | zone3 |
                        |  P P  |  P P  |  P P  |
                        The number of domains is less than 5(MinDomains), so "global minimum" is treated as 0.
                        In this situation, new pod with the same labelSelector cannot be scheduled,
                        because computed skew will be 3(3 - 0) if new Pod is scheduled to any of the three zones,
                        it will violate MaxSkew.
                      format: int32
                      type: integer
                    nodeAffinityPolicy:
                      description: |-
                        NodeAffinityPolicy indicates how we will treat Pod's nodeAffinity/nodeSelector
                        when calculating pod topology spread skew. Options are:
                        - Honor: only nodes matching nodeAffinity/nodeSelector are included in the calculations.
                        - Ignore: nodeAffinity/nodeSelector are ignored. All nodes are included in the calculations.

                        If this value is nil, the behavior is equivalent to the Honor policy.
                      type: string
                    nodeTaintsPolicy:
                      description: |-
                        NodeTaintsPolicy indicates how we will treat node taints when calculating
                        pod topology spread skew. Options are:
                        - Honor: nodes without taints, along with tainted nodes for which the incoming pod
                        has a toleration, are included.
                        - Ignore: node taints are ignored. All nodes are included.

                        If this value is nil, the behavior is equivalent to the Ignore policy.
                      type: string
                    topologyKey:
                      description: |-
                        TopologyKey is the key of node labels. Nodes that have a label with this key
                        and identical values are considered to be in the same topology.
                        We consider each <key, value> as a "bucket", and try to put balanced number
                        of pods into each bucket.
                        We define a domain as a particular instance of a topology.
                        Also, we define an eligible domain as a domain whose nodes meet the requirements of
                        nodeAffinityPolicy and nodeTaintsPolicy.
                        e.g. If TopologyKey is "kubernetes.io/hostname", each Node is a domain of that topology.
                        And, if TopologyKey is "topology.kubernetes.io/zone", each zone is a domain of that topology.
                        It's a required field.
                      type: string
                    whenUnsatisfiable:
                      description: |-
                        WhenUnsatisfiable indicates how to deal with a pod if it doesn't satisfy
                        the spread constraint.
                        - DoNotSchedule (default) tells the scheduler not to schedule it.
                        - ScheduleAnyway tells the scheduler to schedule the pod in any location,
                          but giving higher precedence to topologies that would help reduce the
                          skew.
                        A constraint is considered "Unsatisfiable" for an incoming pod
                        if and only if every possible node assignment for that pod would violate
                        "MaxSkew" on some topology.
                        For example, in a 3-zone cluster, MaxSkew is set to 1, and pods with the same
                        labelSelector spread as 3/1/1:
                        | zone1 | zone2 | zone3 |
                        | P P P |   P   |   P   |
                        If WhenUnsatisfiable is set to DoNotSchedule, incoming pod can only be scheduled
                        to zone2(zone3) to become 3/2/1(3/1/2) as ActualSkew(2-1) on zone2(zone3) satisfies
                        MaxSkew(1). In other words, the cluster can still be imbalanced, but scheduler
                        won't make it *more* imbalanced.
                        It's a required field.
                      type: string
                  required:
                  - maxSkew
                  - topologyKey
                  - whenUnsatisfiable
                  type: object
                type: array
            type: object
            x-kubernetes-validations:
            - message: shards is required when topology is Cluster
              rule: self.topology != 'Cluster' || has(self.shards)
            - message: shards is only valid when topology is Cluster
              rule: self.topology == 'Cluster' || !has(self.shards)
            - message: replicasPerShard is only valid when topology is Cluster
              rule: self.topology == 'Cluster' || !has(self.replicasPerShard)
            - message: sentinel is required when topology is Sentinel
              rule: self.topology != 'Sentinel' || has(self.sentinel)
            - message: sentinel is only valid when topology is Sentinel
              rule: self.topology == 'Sentinel' || !has(self.sentinel)
            - message: replicateFrom only supported for Standalone or Replication
                topology
              rule: '!has(self.replicateFrom) || self.topology == ''Standalone'' ||
                self.topology == ''Replication'''
            - message: replicateFrom.caSecret requires replicateFrom.tls=true and
                spec.tls.enabled=true
              rule: '!has(self.replicateFrom) || !has(self.replicateFrom.caSecret)
                || (self.replicateFrom.tls && has(self.tls) && self.tls.enabled)'
            - message: storage.medium=Memory is ephemeral and not allowed with the
                Durable profile
              rule: '!has(self.storage) || !has(self.storage.medium) || self.storage.medium
                != ''Memory'' || self.profile != ''Durable'''
            - message: perShardWorkload is only valid when topology is Cluster
              rule: '!has(self.perShardWorkload) || self.topology == ''Cluster'''
            - message: shards can only grow unless perShardWorkload=true (per-StatefulSet
                layout supports clean shard scale-down; single-STS removes replicas,
                not a shard — see ADR 0005 / C4)
              rule: '!has(self.shards) || !has(oldSelf.shards) || self.shards >= oldSelf.shards
                || (has(self.perShardWorkload) && self.perShardWorkload)'
            - message: perShardWorkload is immutable
              rule: has(self.perShardWorkload) == has(oldSelf.perShardWorkload) &&
                (!has(self.perShardWorkload) || self.perShardWorkload == oldSelf.perShardWorkload)
          status:
            description: ValkeyClusterStatus defines the observed state of ValkeyCluster.
            properties:
              clusterInitialized:
                description: |-
                  ClusterInitialized is true once `valkey-cli --cluster create` succeeded
                  for Cluster topology. The bootstrap is then never repeated.
                type: boolean
              conditions:
                description: |-
                  Conditions represent the current state of the resource.
                  Well-known types: Available, Progressing, Degraded, ClusterInitialized.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              internalEndpoint:
                description: |-
                  InternalEndpoint is the in-cluster client endpoint (host:port) of the
                  client Service, for consumers to connect to. Surfaced here so wrappers
                  (e.g. a Crossplane Composition) can project it without recomputing names.
                type: string
              lastAppliedReplicas:
                description: |-
                  LastAppliedReplicas is the total replica count last successfully
                  reflected in the running cluster (Cluster topology). Drives scale-up
                  detection: when totalReplicas(spec) > LastAppliedReplicas the operator
                  runs a one-shot add-node Job, then advances this counter.
                format: int32
                type: integer
              lastFailoverToken:
                description: |-
                  LastFailoverToken is the value of the valkey.wellcake.io/failover annotation
                  the operator last acted on (Replication topology). Used to run a manual
                  failover exactly once per request.
                type: string
              lastPasswordRotationToken:
                description: |-
                  LastPasswordRotationToken is the value of the valkey.wellcake.io/rotate-password
                  annotation the operator last acted on. Used to run an in-place password
                  rotation (live CONFIG SET requirepass/masterauth, no restart) exactly once
                  per request.
                type: string
              lastReshardToken:
                description: |-
                  LastReshardToken is the value of the valkey.wellcake.io/reshard annotation the
                  operator last acted on (Cluster topology). Used to run a manual reshard
                  exactly once per request.
                type: string
              lastTLSCertFingerprint:
                description: |-
                  LastTLSCertFingerprint is the SHA-256 fingerprint of the TLS leaf cert the
                  operator last reloaded onto the live pods. When cert-manager renews the
                  cert Secret this no longer matches, triggering an in-place TLS reload
                  (live CONFIG SET tls-cert-file/tls-key-file/tls-ca-cert-file, no restart).
                type: string
              observedGeneration:
                description: ObservedGeneration is the .metadata.generation last reconciled.
                format: int64
                type: integer
              phase:
                description: Phase is a quick high-level summary.
                enum:
                - Pending
                - Creating
                - Ready
                - Updating
                - Failed
                - Hibernated
                type: string
              primary:
                description: Primary holds the pod currently acting as primary (Replication/Sentinel
                  topologies).
                type: string
              primaryDownSince:
                description: |-
                  PrimaryDownSince is when the operator first observed the primary
                  unreachable. It debounces operator-driven failover: a replica is promoted
                  only after the primary has been down for at least the failover threshold,
                  so a primary that is merely briefly busy (slow BGSAVE, GC pause) is not
                  prematurely failed over. Cleared once a master is reachable again.
                format: date-time
                type: string
              readyReplicas:
                description: ReadyReplicas is the count of ready Valkey pods.
                format: int32
                type: integer
              readyShards:
                description: ReadyShards reports how many shards have a Ready primary
                  (Cluster topology only).
                format: int32
                type: integer
              shardDetails:
                description: |-
                  ShardDetails reports per-shard topology, slot ownership and health
                  (Cluster topology only). Refreshed by the reconciler on every pass
                  via `CLUSTER NODES`. Empty when topology != Cluster or the cluster
                  has not bootstrapped yet.
                items:
                  description: |-
                    ShardStatus describes a single shard (one primary plus its replicas) in a
                    Cluster-topology ValkeyCluster.
                  properties:
                    health:
                      description: |-
                        Health summarises the shard state: Ready, Degraded (primary up, some
                        replica down), Down (primary unreachable). Unknown when the survey
                        couldn't talk to any pod.
                      enum:
                      - Unknown
                      - Ready
                      - Degraded
                      - Down
                      type: string
                    index:
                      description: |-
                        Index is the operator-assigned shard ordinal, stable across reconciles.
                        It is the order in which we encountered the master in `CLUSTER NODES`,
                        not a Valkey-internal identifier.
                      format: int32
                      type: integer
                    maxLagBytes:
                      description: |-
                        MaxLagBytes is the largest (primaryOffset - replicaOffset) across the
                        shard's replicas. Equals 0 when there are no replicas or every replica
                        matches the primary exactly.
                      format: int64
                      type: integer
                    primary:
                      description: Primary is the pod name (e.g. "demo-3") that owns
                        this shard's slots.
                      type: string
                    primaryNodeID:
                      description: |-
                        PrimaryNodeID is the Valkey-internal cluster node ID of the primary.
                        Useful for `valkey-cli --cluster reshard --cluster-from <id>` operations.
                      type: string
                    primaryOffset:
                      description: |-
                        PrimaryOffset is the primary's master_repl_offset at the last survey,
                        reported by INFO replication. Useful as a freshness signal and as the
                        baseline for replica lag below.
                      format: int64
                      type: integer
                    replicaOffsets:
                      description: |-
                        ReplicaOffsets enumerates each replica's repl offset alongside the
                        primary's, so lag can be calculated per replica.
                      items:
                        description: ReplicaOffset records one replica's repl offset
                          alongside its pod name.
                        properties:
                          offset:
                            format: int64
                            type: integer
                          pod:
                            type: string
                        required:
                        - offset
                        - pod
                        type: object
                      type: array
                    replicas:
                      description: Replicas lists pod names that replicate this primary.
                      items:
                        type: string
                      type: array
                    slotCount:
                      description: SlotCount is the total slot count owned by this
                        shard.
                      format: int32
                      type: integer
                    slotRanges:
                      description: |-
                        SlotRanges enumerates the slot ranges owned by the primary, e.g.
                        ["0-5460", "12000-12100"]. A shard with zero slots (freshly added by
                        scale-up but not yet reshard'ed) shows an empty list.
                      items:
                        type: string
                      type: array
                  required:
                  - index
                  - primary
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - index
                x-kubernetes-list-type: map
              shards:
                description: Shards reports the configured number of shards (Cluster
                  topology only).
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
