# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/*
Dockerfile.cross

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Go workspace file
go.work

# Vendored dependencies — this project uses Go modules, not vendoring.
/vendor/

# editor and IDE paraphernalia
.idea
.vscode
*.swp
*.swo
*~

# Kubeconfig might contain secrets
*.kubeconfig

# kubebuilder helm/kustomize plugin output (generated, not committed);
# dist/krew/valkey.yaml is a hand-maintained template and stays tracked.
dist/chart/
dist/install.yaml
